# 💬 Feedback Loop Implementation

## Огляд

Система зворотного зв'язку (Feedback Loop) для отримання якісного зворотного зв'язку від користувачів без спаму, без навантаження на підтримку і без складних форм.

## ✨ Ключові принципи

- **Простота**: Мінімум кроків для залишення відгуку
- **Добровільність**: Користувач сам вирішує залишати відгук чи ні
- **Швидкість**: Весь процес займає менше 30 секунд
- **Без реєстрації**: Не потрібно вводити контактні дані

## 📍 Точки збору feedback

### 1. Головне меню
Кнопка **💬 Зворотний зв'язок** доступна в головному меню бота.

### 2. Після скасування (Contextual)
Після скасування налаштування каналу користувачу пропонується залишити відгук:
- Коротке запитання: "Що було незрозуміло?"
- Опціонально: можна пропустити

### 3. Після помилки (Готово до використання)
Якщо виникає помилка, можна викликати `offerFeedbackAfterError()`:
- Коротке запитання: "Хочете повідомити про проблему?"
- Опціонально: можна пропустити

## 🎯 Типи feedback

Користувач обирає один з трьох типів:

1. **🐞 Помилка** - для повідомлення про баги
2. **🤔 Незрозуміло** - коли щось незрозуміло в інтерфейсі
3. **💡 Ідея** - пропозиції покращень

## 🔄 Процес feedback

```
1. Користувач натискає "💬 Зворотний зв'язок"
   ↓
2. Обирає тип (🐞/🤔/💡)
   ↓
3. Надсилає одне текстове повідомлення
   ↓
4. Отримує подяку та повертається в меню
```

## 🛡️ Захист від зловживань (Anti-abuse)

### Rate Limiting
- **1 відгук на 5 хвилин** - запобігає спаму
- **Максимум 10 відгуків на день** - захист від флуду

### Управління станом
- **Timeout 30 хвилин** - автоматичне очищення незавершених сесій
- **Скасування в будь-який момент** - кнопка "❌ Скасувати"

## 💾 Зберігання даних

### Таблиця `feedback`
```sql
- id: унікальний ідентифікатор
- telegram_id: ID користувача Telegram
- username: username користувача (якщо є)
- feedback_type: тип (bug/unclear/idea)
- feedback_text: текст відгуку
- context_type: контекст (wizard_cancel/error/null)
- context_data: додаткові дані контексту (JSON)
- created_at: час створення
```

### Таблиця `feedback_stats`
```sql
- telegram_id: ID користувача
- last_feedback_at: час останнього відгуку
- feedback_count_today: кількість відгуків сьогодні
- last_count_reset: час останнього скидання лічильника
```

## 📊 Аналітика

Модуль `src/feedbackAnalytics.js` надає функції для аналізу:

### Базові функції

1. **getFeedbackSummary(days)** - підсумок за період
   ```javascript
   const summary = getFeedbackSummary(7); // останні 7 днів
   // Returns: { period, counts: { bug, unclear, idea, total }, feedback }
   ```

2. **getGroupedFeedback(limit)** - групування по типах
   ```javascript
   const grouped = getGroupedFeedback(100);
   // Returns: { bugs: [], unclear: [], ideas: [], total }
   ```

3. **detectFeedbackSpike(windowMinutes, threshold)** - виявлення сплеску
   ```javascript
   const spike = detectFeedbackSpike(60, 3);
   // Returns: { isSpike, currentRate, averageRate, ... }
   ```

4. **checkCriticalIssues(windowMinutes, threshold)** - критичні проблеми
   ```javascript
   const critical = checkCriticalIssues(60, 5);
   // Returns: { isCritical, bugCount, bugs: [...] }
   ```

### Формат для адміністратора

```javascript
const feedbackHandler = require('./src/feedbackAnalytics');
const grouped = feedbackHandler.getGroupedFeedback(50);
const formatted = feedbackHandler.formatFeedbackForAdmin(grouped.bugs);
console.log(formatted);
```

## 🚨 Алерти

Система автоматично відслідковує:

### Коли алертити:
- **Spike feedback** - різке зростання відгуків (>3x від середнього)
- **Багато bug reports** - >5 повідомлень про помилки за годину

### Коли НЕ алертити:
- Звичайний feedback - для звичайного аналізу
- Поодинокі відгуки - не реагувати на випадкові повідомлення

## 🔌 API / Використання

### У коді бота

```javascript
// Імпорт
const { 
  handleFeedback, 
  offerFeedbackAfterCancel, 
  offerFeedbackAfterError 
} = require('./handlers/feedback');

// Виклик головного feedback flow
await handleFeedback(bot, msg);

// Контекстний feedback після скасування
await offerFeedbackAfterCancel(bot, chatId, telegramId);

// Контекстний feedback після помилки
await offerFeedbackAfterError(bot, chatId, telegramId, 'api_error');
```

### Callback handlers (вже інтегровано в bot.js)

```javascript
// Обробка feedback callbacks
if (data.startsWith('feedback_') || 
    data === 'offer_feedback_cancel' ||
    data === 'offer_feedback_error') {
  await handleFeedbackCallback(bot, query);
}
```

### Message handler (вже інтегровано в bot.js)

```javascript
// Обробка текстового вводу
const feedbackHandled = await handleFeedbackConversation(bot, msg);
if (feedbackHandled) return;
```

## 📱 UX-повідомлення

### Тон
- Вдячний: "Дякую! 🙌"
- Спокійний: без панічних повідомлень
- Без обіцянок: не обіцяємо що виправимо негайно

### Приклади

**Після відправки:**
```
🙌 Дякую!

Це допоможе зробити бота кращим.
```

**Rate limit:**
```
⏳ Зачекайте 3 хв перед наступним відгуком.

Це допомагає уникнути спаму.
```

**Daily limit:**
```
📊 Досягнуто ліміт відгуків на сьогодні (10).

Спробуйте завтра.
```

## 🧪 Тестування

### Структурні тести
```bash
node test-feedback-structure.js
```

### Повні тести (потребує БД)
```bash
node test-feedback.js
```

## 📂 Файли

```
src/
├── handlers/
│   └── feedback.js          # Основна логіка feedback
├── keyboards/
│   └── inline.js            # Клавіатури для feedback
├── database/
│   └── db.js                # Функції БД для feedback
├── state/
│   └── stateManager.js      # Управління станом feedback
├── feedbackAnalytics.js     # Аналітика feedback
└── bot.js                   # Інтеграція в бота

test-feedback.js             # Тести
test-feedback-structure.js   # Структурні тести
```

## 🎯 Definition of Done

Feedback loop вважається готовим, якщо:

- ✅ Користувачу легко залишити відгук
- ✅ Feedback не заважає користуванню ботом
- ✅ Дані структуровані та зберігаються в БД
- ✅ Є rate limiting для захисту від спаму
- ✅ Є базова аналітика для виявлення проблем
- ✅ Видно повторювані проблеми через групування
- ✅ Система не отримує шум (є anti-abuse)

## 💡 Майбутні покращення

- Додати ML для автоматичного групування схожих відгуків
- Інтеграція з системою тікетів для критичних проблем
- Автоматичне виявлення популярних запитів для FAQ
- Dashboard для візуалізації аналітики

## 📖 Посилання

- Основний код: `src/handlers/feedback.js`
- Аналітика: `src/feedbackAnalytics.js`
- База даних: `src/database/db.js` (функції feedback)

---

**Примітка:** Feedback loop — це не підтримка. Це інструмент розвитку для покращення продукту.
