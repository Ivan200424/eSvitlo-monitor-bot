# 🌐 Multi-Tenant Моніторинг

## Варіант А: Моніторинг роутера через Інтернет

Бот перевіряє доступність роутера користувача через його публічну IP-адресу в Інтернеті.

### ⚠️ Важливо: Потрібна статична IP-адреса

**Динамічна IP-адреса не підходить** — вона змінюється при кожному перезавантаженні роутера, і бот втратить зв'язок з вашим пристроєм.

Вам необхідно замовити у свого інтернет-провайдера послугу **«Статична IP-адреса»**.

| Що потрібно знати | Деталі |
|---|---|
| 💰 Вартість | приблизно 30–50 грн/місяць (залежить від провайдера) |
| 📞 Як замовити | Зверніться до служби підтримки вашого провайдера |
| ℹ️ Статична IP = Публічна IP | Це одне й те саме |

### 🔍 Як дізнатися свою IP-адресу?

Після підключення статичної IP-адреси перевірте її одним із способів:
- Введіть у браузері: **«дізнатися мою IP адресу»** або **«my ip»**
- Перейдіть на сайт: [whatismyipaddress.com](https://whatismyipaddress.com) або [2ip.ua](https://2ip.ua)

### ⚙️ Налаштування роутера для доступу з Інтернету

Щоб бот міг «достукатися» до вашого роутера ззовні, потрібно:

1. **Увімкнути відповідь на ping (ICMP)** — у налаштуваннях роутера знайдіть опцію «Respond to Ping from WAN» або «ICMP Echo Reply» і увімкніть її
2. **Або налаштувати веб-інтерфейс роутера** — переконайтеся, що веб-інтерфейс роутера доступний з WAN на порту 80 або іншому порту
3. **Перевірити Firewall** — переконайтеся, що брандмауер не блокує вхідні HTTP-запити на налаштований порт

> 📍 Розташування цих налаштувань залежить від моделі роутера. Зазвичай вони знаходяться в розділах: **Security**, **Firewall**, **WAN** або **Advanced Settings**.

### 🔧 Налаштування змінних середовища

Після налаштування роутера додайте наступні змінні у файл `.env`:

```bash
# IP-адреса вашого роутера (статична публічна IP)
ROUTER_HOST=192.168.1.1

# Порт роутера (за замовчуванням 80 для веб-інтерфейсу)
ROUTER_PORT=80

# Інтервал перевірки в секундах (за замовчуванням 30)
POWER_CHECK_INTERVAL=30
```

### ✅ Фінальний крок

Після виконання всіх налаштувань:
1. Запустіть або перезапустіть бота
2. Натисніть кнопку **⚡ Світло** в меню бота
3. Бот покаже поточний стан наявності світла

### 🔍 Як це працює

Бот відправляє HTTP HEAD запит на вказану IP-адресу та порт роутера. Якщо роутер відповідає - світло є, якщо немає відповіді протягом 10 секунд - світла немає.

**Переваги:**
- ✅ Автоматичний моніторинг наявності світла
- ✅ Не потрібно встановлювати додаткове обладнання
- ✅ Працює з будь-яким роутером

**Обмеження:**
- ⚠️ Потрібна статична IP-адреса
- ⚠️ Потрібно налаштувати роутер для доступу з Інтернету
- ⚠️ Якщо інтернет-провайдер працює при відключенні світла (наприклад, через резервне живлення), моніторинг може показувати хибні результати
