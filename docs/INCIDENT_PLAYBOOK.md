# 🚑 Incident Playbook

**Проєкт:** eSvitlo-monitor-bot  
**Призначення:** Контрольована реакція на інциденти в продакшені без паніки та хаосу

---

## 🎯 Головний принцип

```
СТАБІЛІЗУВАТИ → ПОЯСНИТИ → ВИПРАВИТИ
```

**Пріоритети:**
1. Зупинити поширення проблеми
2. Захистити користувачів
3. Зберегти довіру
4. Виправити причину
5. Запобігти повторенню

---

## 📊 Класифікація інцидентів

### 🟢 Minor (Незначний)

**Характеристики:**
- Поодинокі баги
- UX дрібниці  
- Не впливають масово на користувачів
- Не блокують критичну функціональність

**Приклади:**
- Один користувач не може підключити канал
- Неправильний текст в одному повідомленні
- Один користувач не отримав notification

**Response time:** 24 години  
**Pause mode:** НЕ потрібен

---

### 🟡 Major (Значний)

**Характеристики:**
- Частина користувачів не може користуватись
- Спам повідомлень або notifications
- Проблеми з графіками або IP моніторингом
- Впливає на UX багатьох користувачів

**Приклади:**
- Scheduler публікує дублікати графіків
- IP моніторинг постійно спамить ON/OFF
- Channel публікації не працюють для всіх
- Графіки не оновлюються

**Response time:** 1-2 години  
**Pause mode:** Розглянути

---

### 🔴 Critical (Критичний)

**Характеристики:**
- Бот повністю не працює або падає
- Масові помилки (error rate > 10%)
- Повна втрата функціоналу
- Data loss або security breach

**Приклади:**
- Процес бота crash loop
- База даних недоступна
- Масовий спам всім користувачам
- Memory leak призводить до падіння
- API keys leaked

**Response time:** Негайно (< 15 хвилин)  
**Pause mode:** ОБОВ'ЯЗКОВО

---

## 🚨 Загальний алгоритм реагування

### Крок 1: Виявлення та фіксація

**Як виявляємо:**
- Monitoring alerts в alert channel
- Повідомлення користувачів
- Помітили самі при тестуванні

**Що робити:**
```bash
# Зафіксуйте інцидент
📝 Час початку: __:__
📊 Джерело: [monitoring/user/self]
🎯 Що сталось: [короткий опис]
```

---

### Крок 2: Оцінка масштабу

**Питання для оцінки:**
- Скільки користувачів зачеплено?
- Чи працює бот взагалі?
- Чи є data loss?
- Чи поширюється проблема?

**Перевірка:**
```bash
# Перевірити логи
/monitoring

# Перевірити метрики
railway logs --tail 100

# Спробувати як користувач
/start (у приватному чаті з ботом)
```

**Класифікуйте:** Minor / Major / Critical

---

### Крок 3: Стабілізація

**Для Critical та Major інцидентів:**

1. **Увімкнути Pause Mode**
   ```
   /pause
   ```
   
2. **Зупинити поширення**
   - Якщо спам → зупинити scheduler
   - Якщо crash → рестарт в контрольованому режимі
   - Якщо memory leak → рестарт + monitoring

3. **Ізолювати проблему**
   - Визначити причину
   - Зупинити проблемний модуль якщо можливо

---

### Крок 4: Комунікація

**Повідомити користувачів (якщо Major або Critical):**

```
🔧 Тимчасова технічна проблема

Зараз працюємо над виправленням.
Деякі функції можуть бути тимчасово недоступні.

Оновлення: [як тільки буде інфо]

Дякуємо за розуміння! 🙏
```

**Що НЕ писати:**
- ❌ "Все зламалось, вибачте"
- ❌ "Не знаємо коли виправимо"
- ❌ "Винні програмісти"
- ❌ Технічні деталі та stack traces

**Що писати:**
- ✅ Коротко та чесно
- ✅ Що робиться для виправлення
- ✅ Приблизний час відновлення (якщо знаєте)
- ✅ Без паніки та виправдань

---

### Крок 5: Виправлення

**Процес:**

1. **Визначити root cause**
   ```bash
   # Аналіз логів
   railway logs --tail 500 | grep ERROR
   
   # Перевірити recent deployments
   git log --oneline -10
   
   # Перевірити monitoring metrics
   /monitoring
   ```

2. **Вибрати стратегію:**
   - **Rollback** - якщо проблема від нового deployment
   - **Hotfix** - якщо можна швидко виправити
   - **Workaround** - тимчасове рішення, постійне виправлення пізніше

3. **Виконати виправлення**

4. **Перевірити на тестових даних**

5. **Поступове відновлення**

---

### Крок 6: Відновлення

**Послідовність:**

1. **Перевірити базові flows**
   ```
   - /start → wizard completion
   - /schedule → shows schedule
   - Channel publication
   - IP monitoring
   ```

2. **Зняти Pause Mode**
   ```
   /resume
   ```

3. **Спостерігати перші 30 хвилин**
   - Monitoring metrics
   - Error rate
   - User feedback

4. **Повідомити про відновлення**
   ```
   ✅ Проблему виправлено
   
   Всі функції працюють у штатному режимі.
   Дякуємо за терпіння! 💚
   ```

---

### Крок 7: Postmortem

**Обов'язково для Major та Critical інцидентів.**

Створити документ з наступними секціями:

```markdown
# Incident Postmortem: [Назва]

## Загальна інформація
- Дата: YYYY-MM-DD
- Час початку: HH:MM
- Час завершення: HH:MM
- Тривалість: X годин
- Severity: Minor/Major/Critical

## Що сталось
[Опис інциденту]

## Timeline
- HH:MM - Виявлено проблему
- HH:MM - Pause mode увімкнено
- HH:MM - Root cause знайдено
- HH:MM - Виправлення застосовано
- HH:MM - Pause mode вимкнено

## Root Cause
[Основна причина]

## Impact
- Користувачів зачеплено: X
- Функції недоступні: [список]
- Data loss: Так/Ні

## Як виявили
[Monitoring alert / User report / Self-detected]

## Що зробили
[Кроки для виправлення]

## Що спрацювало добре
- [Список]

## Що можна покращити
- [Список]

## Action Items
- [ ] [Конкретна дія для запобігання]
- [ ] [Покращення моніторингу]
- [ ] [Оновлення документації]

## Lessons Learned
[Висновки]
```

**Без пошуку винних. Тільки покращення системи.**

---

## 🎭 Специфічні Playbooks

### 🔴 Playbook: Бот падає (Crash Loop)

**Симптоми:**
- Процес постійно перезапускається
- "Application failed to respond" в Railway
- Memory errors у логах
- Бот не відповідає на команди

**Дії:**

1. **Негайно увімкнути Pause Mode** (якщо можливо)
   ```
   /pause
   ```

2. **Перевірити логи**
   ```bash
   railway logs --tail 100
   ```
   Шукати:
   - Out of memory errors
   - Unhandled exceptions
   - Database connection errors

3. **Перевірити system resources**
   ```bash
   # Через Railway dashboard
   # Memory usage
   # CPU usage
   # Database connections
   ```

4. **Рестарт у контрольованому режимі**
   ```bash
   # Railway: Restart service
   # Стежити за логами під час старту
   ```

5. **Якщо memory leak:**
   - Зробити heap dump (якщо можливо)
   - Перевірити recent code changes
   - Розглянути rollback

6. **Після стабілізації:**
   - Перевірити базові flows
   - Зняти pause
   - Моніторити memory usage

**Повідомлення користувачам:**
```
🔧 Технічне обслуговування

Бот тимчасово недоступний через технічні роботи.
Працюємо над відновленням.

Вибачте за незручності.
```

---

### 🔴 Playbook: Спам повідомлень

**Симптоми:**
- Користувачі отримують багато однакових повідомлень
- Scheduler публікує дублікати
- IP monitoring спамить ON/OFF transitions

**Дії:**

1. **Pause Mode негайно**
   ```
   /pause
   ```

2. **Зупинити scheduler/IP engine**
   ```javascript
   // Через адмін панель або код:
   scheduler.stop()
   ipEngine.stop()
   ```

3. **Перевірити причину:**
   
   **Для графіків:**
   ```bash
   # Перевірити hash logic
   # Перевірити snapshot comparison
   # Перевірити scheduler interval
   ```
   
   **Для IP моніторингу:**
   ```bash
   # Перевірити debounce logic
   # Перевірити ping stability
   # Перевірити state transitions
   ```

4. **Виправити код**
   - Hash comparison
   - Debounce intervals
   - State management

5. **Тестування на тестових даних**
   ```bash
   node test-debounce.js
   node test-implementation.js
   ```

6. **Поступове відновлення**
   - Запустити scheduler з більшим interval
   - Стежити за публікаціями
   - Поступово зменшити interval

**Повідомлення користувачам:**
```
🔔 Тимчасово відключені notifications

Виявили проблему з дублюванням повідомлень.
Виправляємо. Notifications відновимо найближчим часом.
```

---

### 🟡 Playbook: Графіки неправильні

**Симптоми:**
- Графіки показують некоректні дані
- Графік "на сьогодні" показує вчорашні дані
- Черги переплутані

**Дії:**

1. **Зупинити публікації**
   ```
   /pause
   ```
   Або вимкнути scheduler для графіків

2. **Перевірити source data**
   ```bash
   # Перевірити API outage-data-ua
   curl https://api.github.com/repos/Baskerville42/outage-data-ua/contents/...
   ```

3. **Перевірити snapshot logic**
   ```javascript
   // Перевірити today/tomorrow визначення
   // Перевірити парсер
   // Перевірити formatter
   ```

4. **Ручна перевірка даних**
   ```bash
   node test-implementation.js
   ```

5. **Виправити та перевірити**
   - Виправити parser/formatter
   - Протестувати на різних regions/groups
   - Порівняти з офіційним джерелом

6. **Обережне відновлення**
   - Запустити для одного test channel
   - Переконатись що коректно
   - Відновити для всіх

**Повідомлення користувачам:**
```
📊 Оновлення графіків тимчасово призупинено

Перевіряємо коректність даних.
Відновимо найближчим часом з перевіреною інформацією.
```

---

### 🟡 Playbook: IP моніторинг не працює

**Симптоми:**
- IP monitoring не визначає стан
- Постійно показує UNKNOWN або UNSTABLE
- Не отримуємо notifications про зміни

**Дії:**

1. **Не panic** - це не критично для основної функціональності

2. **Вимкнути IP engine тимчасово**
   ```javascript
   // Залишити тільки графіки
   ipEngine.stop()
   ```

3. **Діагностика:**
   ```bash
   # Перевірити ping logic
   node test-ip-ddns-feature.js
   
   # Перевірити debounce
   # Перевірити network connectivity
   ```

4. **Виправити проблему:**
   - Ping timeout
   - Debounce intervals
   - State transition logic
   - Network issues

5. **Тестування**
   ```bash
   # Test на різних IP
   # Test на stable/unstable connections
   ```

6. **Увімкнути назад**
   - Поступово
   - Спочатку для admin
   - Потім для всіх

**Повідомлення користувачам:**
```
⚡ IP моніторинг тимчасово недоступний

Графіки та всі інші функції працюють нормально.
Працюємо над відновленням IP моніторингу.
```

---

### 🟡 Playbook: Database проблеми

**Симптоми:**
- "Database locked" errors
- Дані не зберігаються
- Повільні запити

**Дії:**

1. **Pause Mode**
   ```
   /pause
   ```

2. **Перевірити database**
   ```bash
   # Перевірити розмір
   railway run ls -lh /app/data/
   
   # Перевірити integrity
   railway run sqlite3 /app/data/bot.db "PRAGMA integrity_check;"
   ```

3. **Перевірити connections**
   ```javascript
   // Скільки відкритих connections?
   // Чи закриваємо connection properly?
   // Connection pool settings
   ```

4. **Можливі рішення:**
   
   **Якщо locked:**
   - Збільшити timeout
   - Перевірити transactions
   - Рестарт бота
   
   **Якщо повільно:**
   - Додати indexes
   - Optimize queries
   - Vacuum database
   
   **Якщо corrupted:**
   - Restore з backup
   - Recreate database

5. **Тестування**
   ```bash
   # Базові операції
   # Read/write performance
   ```

**Повідомлення користувачам:**
```
🔧 Технічні роботи з базою даних

Деякі дані можуть бути тимчасово недоступні.
Працюємо над відновленням.
```

---

### 🟢 Playbook: Окремі user complaints

**Симптоми:**
- Один користувач скаржиться на проблему
- Інші користувачі працюють нормально

**Дії:**

1. **НЕ pause mode** - це не масова проблема

2. **Зібрати інформацію:**
   ```
   - User ID
   - Що саме не працює
   - Які кроки робив
   - Screenshot якщо можливо
   ```

3. **Перевірити user state**
   ```javascript
   // Подивитись user record у БД
   // Перевірити active_state
   // Перевірити region/group
   ```

4. **Спробувати reproduce**
   ```bash
   # Те саме що робив користувач
   # На тому самому region/group
   ```

5. **Можливі рішення:**
   
   **Якщо stuck state:**
   ```javascript
   // Clear user state
   stateManager.clear(userId)
   ```
   
   **Якщо bad data:**
   ```javascript
   // Fix user data
   // Reset to default
   ```

6. **Follow up з користувачем**
   ```
   Виправлено. Спробуйте ще раз будь ласка.
   Якщо проблема повториться - повідомте.
   ```

**Monitoring:**
- Чи повторюється у інших?
- Чи є pattern?

---

## 📢 Комунікаційні шаблони

### ✅ Хороші приклади

**Critical incident start:**
```
🔧 Важлива інформація

Зараз спостерігаємо технічну проблему.
Працюємо над виправленням у пріоритетному режимі.

Деякі функції можуть бути тимчасово недоступні.

Оновлення: протягом години.
```

**Major incident start:**
```
🔔 Тимчасові технічні роботи

Виявили проблему з [графіками/notifications].
Зараз виправляємо.

Інші функції працюють нормально.

Дякуємо за розуміння.
```

**Incident resolved:**
```
✅ Проблему виправлено

Всі функції працюють у штатному режимі.

Вибачте за тимчасові незручності.
Дякуємо за терпіння! 💚
```

**Minor issue:**
```
🔧 Незначне покращення

Виправили дрібну проблему з [функція].
Все працює як треба.
```

---

### ❌ Погані приклади

**НЕ писати:**

```
❌ "Все зламалось"
❌ "Не знаємо що сталося"
❌ "Вибачте що так вийшло"
❌ "Error: NullPointerException at line 234"
❌ "Хтось накосячив з кодом"
❌ "Не знаємо коли виправимо"
❌ "Спробуйте пізніше (коли саме - невідомо)"
```

**Чому погано:**
- Панічні формулювання
- Технічний жаргон
- Пошук винних
- Невизначеність
- Втрата довіри

---

## 📊 Post-Incident Metrics

**Що вимірювати після інциденту:**

- **MTTD** (Mean Time To Detect) - час до виявлення
- **MTTR** (Mean Time To Resolve) - час до вирішення
- **MTBF** (Mean Time Between Failures) - час між збоями
- **Impact** - скільки користувачів зачеплено
- **Recovery Quality** - чи були повторні проблеми

**Цільові показники:**
- MTTD < 15 хвилин (через monitoring)
- MTTR < 1 година (для Major)
- MTTR < 15 хвилин (для Critical з rollback)
- Impact < 10% користувачів (для Major)

---

## 🎓 Навчання на помилках

**Після кожного інциденту:**

1. **Що спрацювало добре?**
   - Monitoring виявив вчасно?
   - Pause mode допоміг?
   - Комунікація була чіткою?

2. **Що можна покращити?**
   - Швидше виявлення?
   - Кращий rollback process?
   - Більше automated checks?

3. **Як запобігти?**
   - Додати до Release Checklist
   - Додати моніторинг
   - Додати тести

4. **Оновити документацію:**
   - Цей playbook
   - Release checklist
   - Architecture guidelines

---

## 🔧 Інструменти для інциденту

### Railway

```bash
# Переглянути логи
railway logs --tail 500

# Рестарт
railway restart

# Rollback deployment
railway redeploy [deployment-id]

# Database backup
railway run cat /app/data/bot.db > backup.db
```

### Git

```bash
# Знайти коли проблема почалась
git log --oneline --since="2 hours ago"

# Rollback до версії
git revert [commit-hash]
git push origin main

# Створити hotfix branch
git checkout -b hotfix/critical-issue
```

### Telegram Bot

```bash
# Адмін команди
/pause          # Pause mode
/resume         # Resume
/monitoring     # Metrics
/stats          # Statistics
/setalertchannel  # Alert channel
```

---

## ✅ Definition of Done (Incident Response)

Incident response готовий, якщо:

- ✅ Класифікація зрозуміла (Minor/Major/Critical)
- ✅ Кожен тип має playbook
- ✅ Комунікаційні шаблони готові
- ✅ Monitoring може виявити проблеми
- ✅ Pause mode працює і доступний
- ✅ Rollback процедура відпрацьована
- ✅ Postmortem process налаштований
- ✅ Ти не боїшся інцидентів

---

## 📚 Додаткові ресурси

- [RELEASE_CHECKLIST.md](RELEASE_CHECKLIST.md) - Чеклист перед релізом
- [MONITORING_GUIDE.md](../MONITORING_GUIDE.md) - Система моніторингу
- [ARCHITECTURE.md](ARCHITECTURE.md) - Архітектура для розуміння системи
- [DEPLOYMENT.md](../DEPLOYMENT.md) - Процедури деплою та rollback

---

## 🎯 Ключові висновки

**Пам'ятайте:**

1. **Швидка стабілізація важливіша за перфектне рішення**
   - Спочатку зупини проблему
   - Потім виправ правильно

2. **Комунікація = довіра**
   - Чесно
   - Коротко
   - Без паніки
   - Без технічних деталей

3. **Навчайся на помилках**
   - Postmortem без звинувачень
   - Покращуй процеси
   - Оновлюй документацію

4. **Інциденти — це нормально**
   - Продакшен не буває ідеальним
   - Важливо як ти реагуєш
   - Кожен інцидент = урок

---

**Продакшен — це не відсутність проблем.**  
**Це вміння з ними працювати професійно та спокійно.**

🚑 **Stay calm. Follow playbook. Learn and improve.**
