# 🧪 Release Checklist

**Проєкт:** eSvitlo-monitor-bot  
**Призначення:** Забезпечення безпечних та передбачуваних релізів без втрати довіри користувачів

---

## 📋 Коли використовувати

Release checklist **ОБОВ'ЯЗКОВИЙ** перед:
- Будь-яким деплоєм у продакшен
- Вмиканням нової функціональності
- Public-launch нових фіч
- Критичними змінами в коді

**ЖОДЕН реліз не дозволений без проходження чеклисту.**

---

## ✅ Чеклист

### 1.1. КОД (ОБОВʼЯЗКОВО)

**Перевірка якості коду:**

- [ ] Немає `TODO` коментарів у production коді
- [ ] Немає `console.log` або debug логів (використовуйте logger)
- [ ] Немає закоментованого коду
- [ ] Немає hardcode значень (токенів, URL, магічних чисел)
- [ ] Handlers не містять бізнес-логіки (лише routing та виклик сервісів)
- [ ] Нові сервіси не залежать напряму від Telegram API
- [ ] Код відповідає архітектурі (див. `docs/ARCHITECTURE.md`)

**Як перевірити:**
```bash
# Пошук TODO
grep -r "TODO\|FIXME\|HACK" src/

# Пошук console.log
grep -r "console\.log" src/

# Пошук закоментованого коду
grep -r "^[[:space:]]*//.*(" src/
```

---

### 1.2. STATE / UX

**Перевірка станів користувача:**

- [ ] У користувача може бути тільки **один активний state** одночасно
- [ ] Всі кнопки "Скасувати" ведуть до `[← Назад]` або `[⤴ Меню]`
- [ ] Немає "тупикових" екранів (звідки неможливо вийти)
- [ ] Timeout коректно повертає користувача в зрозумілий стан
- [ ] State cleanup працює правильно при завершенні wizard
- [ ] Немає race condition при швидких кліках

**Як перевірити:**
- Пройдіть усі user flows вручну
- Спробуйте швидко натискати кнопки
- Перевірте timeout сценарії (зачекайте 5+ хвилин)
- Спробуйте `/start` в середині wizard

---

### 1.3. ГРАФІКИ

**Перевірка логіки графіків:**

- [ ] `today` / `tomorrow` snapshot логіка працює коректно
- [ ] Hash порівняння працює (немає публікацій при однакових даних)
- [ ] Немає дубльованих публікацій графіків
- [ ] Заголовки коректні: "Графік на сьогодні", "Графік оновлено", "Зʼявився графік"
- [ ] Scheduler працює з правильним інтервалом
- [ ] Обробка помилок API outage-data-ua

**Як перевірити:**
```bash
# Перевірити scheduler логіку
node test-implementation.js

# Мануальна перевірка публікацій
# 1. Підключіть тестовий канал
# 2. Зачекайте інтервал перевірки
# 3. Переконайтесь що немає дублів
```

---

### 1.4. IP-МОНІТОРИНГ

**Перевірка IP engine:**

- [ ] Debounce працює (немає частих змін ON/OFF)
- [ ] Немає спаму повідомлень при нестабільному з'єднанні
- [ ] Некоректні IP адреси блокуються або валідуються
- [ ] IP логіка НЕ викликається безпосередньо з handlers
- [ ] Ping timeout налаштований правильно
- [ ] UNSTABLE стан обробляється коректно

**Як перевірити:**
```bash
# Перевірити IP валідацію
node test-ip-ddns-feature.js

# Мануальна перевірка:
# 1. Введіть правильний IP
# 2. Введіть некоректний IP (перевірка валідації)
# 3. Симулюйте нестабільне з'єднання (вимкніть/ввімкніть роутер)
```

---

### 1.5. ANTI-ABUSE

**Перевірка захисту від зловживань:**

- [ ] Rate-limit активний на критичних діях
- [ ] Cooldown працює (користувач не може спамити)
- [ ] Flood protection захищає від масових запитів
- [ ] Повторні дії не ламають state
- [ ] Quick clicks оброблюються gracefully
- [ ] Захист від одночасних wizard sessions

**Як перевірити:**
- Швидко натискайте кнопки (10+ разів за секунду)
- Спробуйте запустити кілька wizard одночасно
- Перевірте логи на anti-abuse події

---

### 1.6. АДМІНКА

**Перевірка адміністративних функцій:**

- [ ] Pause mode вмикається і вимикається коректно
- [ ] Pause блокує критичні дії користувачів
- [ ] Pause події логуються
- [ ] Зміна debounce/інтервалів застосовується без рестарту бота
- [ ] Адмін-команди доступні тільки адмінам
- [ ] `/monitoring` показує актуальні метрики

**Як перевірити:**
```bash
# Адмін команди
/pause  # Увімкнути паузу
/resume # Вимкнути паузу
/monitoring # Перевірити метрики
/stats # Статистика
```

**Перевірити:**
- Увімкніть pause, спробуйте виконати дії як звичайний користувач
- Змініть інтервал, переконайтесь що працює без рестарту
- Перевірте логи адмін дій

---

### 1.7. ТЕСТИ

**Перевірка тестового покриття:**

- [ ] Unit тести проходять без помилок
- [ ] Integration флоу перевірені вручну
- [ ] Критичні сценарії протестовані:
  - `/start` → wizard → completion
  - Wizard cancel на різних етапах
  - IP setup та моніторинг
  - Channel connect/disconnect
  - Pause mode вмикання/вимикання
  - Scheduler робота

**Як перевірити:**
```bash
# Запустити всі тести
npm test

# Або окремо
node test.js
node test-implementation.js
node test-ip-ddns-feature.js
node test-state-persistence.js
```

---

### 1.8. MONITORING

**Перевірка готовності моніторингу:**

- [ ] Логи пишуться в правильному форматі
- [ ] Алерти активні та налаштовані
- [ ] Alert channel доступний та бот є адміном
- [ ] Метрики збираються (system, application, business, UX)
- [ ] Pause можна ввімкнути за < 1 хвилину
- [ ] Health check endpoint працює (якщо є)

**Як перевірити:**
```bash
# Перевірити monitoring
/monitoring

# Налаштувати alert channel (якщо ще не налаштований)
/setalertchannel @your_alerts_channel

# Перевірити тестовий алерт
node test-monitoring.js
```

**Див. також:** `MONITORING_GUIDE.md`

---

### 1.9. RELEASE DECISION

**Фінальне рішення про реліз:**

Реліз **ДОЗВОЛЕНИЙ** тільки якщо:

✅ Немає жодних сумнівів у стабільності  
✅ Відсутні коментарі "потім поправлю"  
✅ Зрозуміло, як відкотитись (rollback plan)  
✅ Є backup бази даних (або volume snapshot)  
✅ Моніторинг готовий виявити проблеми  
✅ Є план комунікації з користувачами при проблемах

Реліз **ЗАБОРОНЕНИЙ** якщо:

❌ Є будь-які сумніви  
❌ Залишились TODO в коді  
❌ Не всі тести пройдені  
❌ Моніторинг не готовий  
❌ Немає плану відкоту

**Якщо є хоч один сумнів — реліз відкладається.**

---

## 🚀 Pre-Deploy Process

### Перед деплоєм:

1. **Завершіть всі пункти чеклисту**
   ```bash
   # Запустіть фінальну перевірку
   npm test
   ```

2. **Створіть backup**
   ```bash
   # Railway: створіть snapshot volume
   # Або: завантажте базу даних
   railway run cat /app/data/bot.db > backup-$(date +%Y%m%d).db
   ```

3. **Підготуйте rollback plan**
   - Визначте попередню робочу версію
   - Переконайтесь що можете швидко відкотитись
   - Підготуйте команди для rollback

4. **Повідомте команду**
   - Час деплою
   - Що змінюється
   - Час спостереження після деплою

5. **Deploy**
   ```bash
   git push origin main
   # Railway автоматично задеплоїть
   ```

6. **Post-deploy monitoring**
   - Перші 15 хвилин: активне спостереження
   - Перевірте `/monitoring`
   - Перевірте логи
   - Перевірте критичні flows як користувач

---

## 📊 Metrics для Release

**Відстежуйте після релізу:**

- Error rate (має бути < 1%)
- User engagement (DAU, WAU)
- Critical actions success rate
- System resources (memory, CPU)
- Response time

**Alarm threshold:**
- Якщо error rate > 5% — розгляньте rollback
- Якщо critical action fails > 10% — rollback
- Якщо memory > 90% — розгляньте rollback

---

## 🔄 Rollback Procedure

Якщо щось пішло не так:

1. **Негайно** увімкніть pause mode:
   ```
   /pause
   ```

2. **Rollback до попередньої версії:**
   - Railway: знайдіть попередній успішний deployment
   - Натисніть "Redeploy"
   - Або: `git revert` + `git push`

3. **Повідомте користувачів:**
   ```
   Тимчасова технічна проблема. 
   Повертаємось до стабільної версії. 
   Працюємо над виправленням.
   ```

4. **Вимкніть pause:**
   ```
   /resume
   ```

5. **Post-rollback:**
   - Зробіть postmortem
   - Виправте проблему
   - Повторіть release checklist

---

## ✅ Definition of Done (Release)

Release процес готовий та дотриманий, якщо:

- ✅ Всі пункти чеклисту виконані
- ✅ Тести пройшли
- ✅ Моніторинг активний
- ✅ Backup створений
- ✅ Rollback plan готовий
- ✅ Команда повідомлена
- ✅ Немає сумнівів

---

## 📚 Додаткові ресурси

- [ARCHITECTURE.md](ARCHITECTURE.md) - Архітектура системи
- [INCIDENT_PLAYBOOK.md](INCIDENT_PLAYBOOK.md) - Інструкції при інцидентах
- [MONITORING_GUIDE.md](../MONITORING_GUIDE.md) - Система моніторингу
- [DEPLOYMENT.md](../DEPLOYMENT.md) - Процес деплою

---

**Пам'ятайте:** Краще затримати реліз на день, ніж зламати продакшен на годину.

**Релізи — це не швидкість. Це передбачуваність та довіра.**
