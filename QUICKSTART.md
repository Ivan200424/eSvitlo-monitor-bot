# 🚀 Quick Start Guide

Швидкий старт для розробників та користувачів бота.

## Для користувачів бота

### Перший запуск

1. **Запустіть бота**: Відправте `/start` боту в Telegram
2. **Оберіть регіон**: Київ / Київська область / Дніпро / Одеса
3. **Оберіть групу**: 1, 2 або 3
4. **Оберіть підгрупу**: X.1 або X.2
5. **Підтвердіть**: Натисніть "✅ Підтвердити"

### Основні команди

```
/schedule або 📋  - Поточний графік відключень
/next або ⏭      - Наступне відключення/включення
/timer або ⏰     - Таймер до події
/settings або ⚙️  - Налаштування
/channel або 📺   - Підключити канал
/help            - Довідка
```

### Підключення каналу

1. Створіть Telegram канал
2. Додайте бота як адміністратора
3. Дайте боту права на публікацію
4. Відправте будь-яке повідомлення в канал
5. Переслідіть це повідомлення боту в приватні повідомлення
6. Готово! Бот почне постити оновлення в канал

### Налаштування алертів

1. Відправте `/settings`
2. Натисніть "🔔 Налаштування сповіщень"
3. Оберіть за скільки хвилин сповіщати (5, 10, 15, 30, 60)
4. Увімкніть/вимкніть сповіщення про відключення та включення

## Для розробників

### Локальна розробка

```bash
# 1. Клонуйте репозиторій
git clone https://github.com/Ivan200424/eSvitlo-monitor-bot.git
cd eSvitlo-monitor-bot

# 2. Встановіть залежності
npm install

# 3. Налаштуйте .env
cp .env.example .env
# Відредагуйте .env та додайте ваш BOT_TOKEN

# 4. Запустіть бота
npm run dev
```

### Тестування

```bash
# Запустити тести
npm test

# Перевірити синтаксис
node -c src/index.js

# Перевірити всі файли
find src -name "*.js" -exec node -c {} \;
```

### Структура файлів

```
src/
├── index.js          # Головний файл
├── bot.js            # Telegram бот
├── config.js         # Конфігурація
├── api.js            # API для отримання даних
├── parser.js         # Парсинг графіків
├── formatter.js      # Форматування повідомлень
├── scheduler.js      # Перевірка графіків
├── alerts.js         # Система алертів
├── utils.js          # Допоміжні функції
│
├── database/
│   ├── db.js         # SQLite з'єднання
│   └── users.js      # Робота з користувачами
│
├── handlers/
│   ├── start.js      # Обробник /start
│   ├── schedule.js   # Обробник графіків
│   ├── settings.js   # Обробник налаштувань
│   ├── channel.js    # Обробник каналів
│   └── admin.js      # Адмін команди
│
├── keyboards/
│   └── inline.js     # Inline клавіатури
│
└── constants/
    └── regions.js    # Регіони та черги
```

### Додавання нового регіону

1. Відредагуйте `src/constants/regions.js`:
```javascript
const REGIONS = {
  // ... існуючі регіони
  lviv: {
    name: 'Львів',
    code: 'lviv',
  },
};
```

2. Переконайтесь, що дані доступні на:
```
https://raw.githubusercontent.com/Baskerville42/outage-data-ua/main/data/lviv.json
```

3. Перезапустіть бота

### Додавання нової команди

1. Створіть обробник в `src/handlers/mycommand.js`:
```javascript
async function handleMyCommand(bot, msg) {
  const chatId = msg.chat.id;
  await bot.sendMessage(chatId, 'Hello!');
}

module.exports = { handleMyCommand };
```

2. Додайте команду в `src/bot.js`:
```javascript
const { handleMyCommand } = require('./handlers/mycommand');
bot.onText(/\/mycommand/, handleMyCommand);
```

3. Оновіть довідку в `src/formatter.js`

### Debug режим

```bash
# Вивід додаткової інформації
NODE_ENV=development npm start
```

### Docker розробка

```bash
# Збірка образу
docker build -t esvitlo-bot-dev .

# Запуск з локальним volume
docker run -it --rm \
  -e BOT_TOKEN=$BOT_TOKEN \
  -e ADMIN_IDS=$ADMIN_IDS \
  -v $(pwd)/data:/app/data \
  esvitlo-bot-dev
```

## Корисні поради

### Отримати свій Telegram ID
Відкрийте [@userinfobot](https://t.me/userinfobot)

### Створити бота
Відкрийте [@BotFather](https://t.me/BotFather) → `/newbot`

### Тестувати callback кнопки
Використовуйте Telegram Desktop для швидшого тестування

### Перегляд логів
```bash
# Локально
npm start

# Docker
docker logs -f container_name

# Railway
railway logs
```

### Очистити базу даних
```bash
rm -f data/bot.db*
npm start  # Створить нову БД
```

## Troubleshooting

**Бот не відповідає:**
- Перевірте токен в .env
- Переконайтесь що бот запущений
- Подивіться логи на помилки

**Помилки бази даних:**
- Видаліть data/*.db та перезапустіть
- Переконайтесь що директорія data/ існує

**Помилки API:**
- Перевірте інтернет-з'єднання
- GitHub може бути недоступний
- Спробуйте пізніше

## Наступні кроки

- 📖 Прочитайте [README.md](README.md) для повної документації
- 🚂 Дивіться [DEPLOYMENT.md](DEPLOYMENT.md) для деплою на Railway
- 🤝 Прочитайте [CONTRIBUTING.md](CONTRIBUTING.md) для участі в розробці

---

Успіхів! 🎉
