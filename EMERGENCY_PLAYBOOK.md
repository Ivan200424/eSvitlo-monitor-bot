# 🚨 EMERGENCY PLAYBOOK
## Процедури для аварійних ситуацій

> **ВАЖЛИВО**: Цей документ містить покрокові інструкції для швидкого реагування на критичні ситуації.  
> **Зберігайте доступ**: Додайте в закладки або роздрукуйте.

---

## 🔴 РІВНІ КРИТИЧНОСТІ

### 🚨 P0 - КРИТИЧНИЙ (негайна дія)
- Бот повністю не працює
- Масова відправка помилок користувачам
- Втрата даних
- Порушення безпеки

**Час реакції**: < 5 хвилин

### ⚠️ P1 - ВИСОКИЙ (швидка дія)
- Окремі фічі не працюють
- Високий error rate (> 10%)
- Повільна робота (> 10 сек відповідь)
- Витік пам'яті

**Час реакції**: < 30 хвилин

### 🟡 P2 - СЕРЕДНІЙ (планова дія)
- Незначні баги
- UX незручності
- Малопоширені помилки

**Час реакції**: < 24 години

---

## 🚨 P0: БОТ НЕ ПРАЦЮЄ

### Симптоми:
- Бот не відповідає на повідомлення
- Error: "bot stopped" в логах
- Railway/hosting показує crash

### Швидка діагностика (2 хв):

```bash
# 1. Перевірити чи працює процес
Railway Dashboard → Deployments → Latest

# 2. Переглянути останні логи
Railway Dashboard → Logs (останні 50 рядків)

# 3. Шукати критичні помилки:
- "Uncaught exception"
- "ECONNREFUSED"
- "Database error"
- "Out of memory"
```

### Процедура відновлення:

#### Крок 1: Термінова пауза (якщо бот частково працює)
```
1. Відкрити бот (якщо доступний)
2. /admin
3. "⏸️ Режим паузи"
4. "Увімкнути паузу"
5. Обрати тип: "🚨 Аварія"
```

#### Крок 2: Швидкий рестарт
```
Railway Dashboard:
1. Перейти в Deployments
2. Знайти останній успішний deployment
3. Натиснути "Redeploy"
або
4. Натиснути "Restart" на поточному deployment
```

#### Крок 3: Перевірка після рестарту (5 хв)
```
1. Зачекати 30 секунд після рестарту
2. Відкрити бот → /start (в новому чаті)
3. Переглянути логи - чи є нові помилки?
4. /admin → Статистика (чи працює?)
5. Тестове підключення каналу (опціонально)
```

#### Крок 4: Вимкнути паузу (якщо все ОК)
```
1. /admin
2. "⏸️ Режим паузи"
3. "Вимкнути паузу"
4. Інформувати користувачів (опціонально через broadcast)
```

### Якщо не допомогло:

```
1. Перевірити Railway статус (https://status.railway.app)
2. Перевірити Telegram API статус (https://status.telegram.org)
3. Перевірити логи на помилки БД
4. Rollback на попередню версію:
   Railway → Deployments → Обрати стабільну версію → Redeploy
5. Зв'язатись з командою/підтримкою Railway
```

---

## ⚠️ P1: МАСОВІ ПОМИЛКИ ВІДПРАВКИ

### Симптоми:
- Логи повні "Failed to send message"
- Користувачі скаржаться на відсутність повідомлень
- Error rate > 10%

### Діагностика:

```javascript
// Перевірити в логах:
"429 Too Many Requests" → Rate limit exceeded
"403 Forbidden" → Bot blocked / channel removed
"400 Bad Request" → Invalid data
```

### Процедура:

#### 1. Rate Limit (429 error)
```
Проблема: Забагато запитів до Telegram API

Швидке вирішення:
1. /admin → Інтервали
2. Збільшити інтервал графіків: 60 → 180 сек
3. Зачекати 5 хвилин
4. Моніторити логи

Довготривале:
1. Переглянути rate limiter конфігурацію
2. Оптимізувати кількість повідомлень
3. Додати black queue
```

#### 2. Масові 403 помилки
```
Проблема: Бот втратив права або користувачі заблокували

Дії:
1. Ігнорувати для окремих користувачів (нормально)
2. Якщо масово (> 50%) → перевірити bot token
3. Деактивувати проблемних користувачів в БД:
   → UPDATE users SET is_active = 0 WHERE ...
```

#### 3. 400 Bad Request
```
Проблема: Некоректні дані в повідомленнях

Дії:
1. Знайти в логах які саме дані проблемні
2. Швидкий фікс: Додати валідацію
3. Відкатити зміни якщо після deploy
```

---

## ⚠️ P1: ВИТІК ПАМ'ЯТІ

### Симптоми:
- Memory continuously growing
- Frequent restarts
- Slow performance over time

### Діагностика:

```
/admin → Система
- Якщо Heap Used наближається до Heap Total
- Якщо RSS постійно зростає

Railway Dashboard → Metrics:
- Memory usage trend стрімко вгору
```

### Швидке вирішення:

```
1. Негайний рестарт:
   Railway → Restart Deployment

2. Моніторити після рестарту (1 година):
   - Якщо пам'ять стабільна → Тимчасово вирішено
   - Якщо знову зростає → Потрібен фікс

3. Тимчасова міра - автоматичні рестарти:
   Налаштувати Railway auto-restart при memory limit
```

### Довготривале вирішення:

```javascript
// Найчастіші причини витоку:

1. Event listeners не видаляються
   → Перевірити bot.on() без .removeListener()

2. Зберігання даних в пам'яті
   → Перевірити in-memory кеші, черги
   → Переконатись що стейти очищаються

3. Circular references
   → Перевірити об'єкти які посилаються один на одного

4. Large string accumulation
   → Перевірити логування, конкатенацію стрінгів
```

---

## ⚠️ P1: БАЗА ДАНИХ НЕДОСТУПНА

### Симптоми:
- "Database locked" errors
- "SQLITE_BUSY" errors
- "Cannot open database"

### Процедура:

```
1. Увімкнути паузу (якщо можливо)

2. Перевірити файл БД:
   Railway → Data → Volumes
   → Перевірити що bot.db існує і доступний

3. Якщо БД locked:
   → Рестарт бота (звільнить lock)
   
4. Якщо БД corrupted:
   → Відновити з backup (якщо є)
   або
   → Створити нову БД (втрата даних)

5. Превентивні міри:
   → Налаштувати автоматичні backup
   → Розглянути PostgreSQL для production
```

---

## ⚠️ P1: КАНАЛИ НЕ ПІДКЛЮЧАЮТЬСЯ

### Симптоми:
- Користувачі не можуть підключити канали
- "Bot not admin" помилки
- "Channel already connected" помилки

### Процедура:

```
1. Перевірити pause mode:
   /admin → Режим паузи
   → Якщо увімкнена - це очікувана поведінка

2. Перевірити права бота:
   - Тестово додати бота в тестовий канал
   - Перевірити чи є права на posting
   
3. Перевірити валідацію:
   - Переглянути логи channel.js
   - Чи є специфічні помилки?

4. Якщо масова проблема:
   → Увімкнути паузу для channel connections
   → Виправити проблему
   → Тестувати
   → Вимкнути паузу
```

---

## 🟡 P2: IP МОНІТОРИНГ "МЕРЕХТИТЬ"

### Симптоми:
- Багато "power on" / "power off" підряд
- Користувачі скаржаться на spam сповіщень

### Процедура:

```
1. Перевірити debounce налаштування:
   /admin → Інтервали
   → Debounce має бути ≥ 5 хвилин

2. Тимчасово збільшити debounce:
   → 5 хв → 10 хв (для проблемних користувачів)

3. Якщо не допомагає:
   → Перевірити мережеву стабільність
   → Запропонувати користувачу збільшити debounce
   → Розглянути adaptive debounce
```

---

## 📋 ШВИДКИЙ ДОВІДНИК КОМАНД

### Адмін команди:
```
/admin              - Адмін панель
/stats              - Швидка статистика
/users              - Список користувачів
/broadcast [текст]  - Розсилка
```

### Критичні дії:
```
Пауза:              /admin → Режим паузи → Увімкнути
Блок реєстрації:    /admin → Контроль росту → Заблокувати
Інтервали:          /admin → Інтервали
Рестарт:            Railway Dashboard → Restart
```

### Перевірка здоров'я:
```
Статистика:         /admin → Статистика v2
Система:            /admin → Система
Логи:               Railway Dashboard → Logs
Metrics:            Railway Dashboard → Metrics
```

---

## 📞 ESCALATION

### Рівень 1: Самостійно
- Використати цей playbook
- Базові рестарти
- Пауза/Resume

### Рівень 2: Команда
- Не можу вирішити за 30 хв
- Потрібен code fix
- Неочікувана поведінка

### Рівень 3: Підтримка платформи
- Railway не працює
- Telegram API проблеми
- Інфраструктурні питання

---

## ✅ POST-INCIDENT CHECKLIST

Після вирішення критичної ситуації:

- [ ] Документувати incident (що, коли, як вирішили)
- [ ] Оновити metrics
- [ ] Інформувати користувачів (якщо був downtime)
- [ ] Провести postmortem (для P0)
- [ ] Додати превентивні міри
- [ ] Оновити цей playbook (якщо потрібно)

---

**Документ створено**: 2026-02-06  
**Версія**: 1.0  
**ТРИМАЙТЕ ПІД РУКОЮ**
