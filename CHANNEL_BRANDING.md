# Channel Branding and Protection Feature

## Огляд

Цей функціонал додає обов'язкове брендування та захист для каналів GridBot. Всі канали отримують єдиний фірмовий стиль, а бот автоматично відстежує та блокує канали з неавторизованими змінами.

## Основні можливості

### 1. Брендування каналів

#### Формат назви каналу
```
GridBot ⚡️ {введення користувача}
```
Приклад: `GridBot ⚡️ Київ Черга 3.1`

- Префікс `GridBot ⚡️ ` додається автоматично
- Користувач вводить тільки свою частину

#### Формат опису каналу

**Обов'язкова частина:**
```
🤖 GridBot — слідкує, щоб ти не слідкував
```

**Опціональна частина:**
```
📍 {опис користувача}
```

**Повний приклад:**
```
🤖 GridBot — слідкує, щоб ти не слідкував
📍 ЖК "Сонячний", під'їзд 2
```

#### Фото каналу
- Використовується файл `photo_for_channels.PNG` з репозиторію
- Встановлюється для ВСІХ каналів автоматично
- `file_id` фото зберігається для перевірки

### 2. Процес налаштування

```
1. /setchannel @mychannel

2. Бот перевіряє права доступу

3. Користувач вводить назву каналу
   Приклад: Київ Черга 3.1

4. Бот пропонує додати опис:
   [✍️ Додати опис] [⏭️ Пропустити]

5а. Якщо "Додати опис":
    Користувач вводить опис

5б. Якщо "Пропустити":
    Використовується тільки базовий опис

6. Бот встановлює:
   - Назву каналу
   - Опис каналу
   - Фото каналу

7. Успішне повідомлення з попередженням
```

### 3. Захист від змін

#### Щоденна перевірка
- **Час:** 03:00 ночі
- **Перевіряється:**
  - Назва каналу
  - Опис каналу
  - Фото каналу (file_id)

#### При виявленні порушення

Користувач отримує повідомлення:
```
⚠️ Виявлено зміни в каналі "GridBot ⚡️ Київ Черга 3.1"

Ви змінили назву/опис/фото каналу, що заборонено 
правилами використання GridBot.

🔴 Моніторинг зупинено.

Щоб відновити роботу, налаштуйте канал заново 
командою /setchannel
```

**Дії бота:**
- Канал блокується (статус → 'blocked')
- Моніторинг зупиняється
- Публікація оновлень припиняється

### 4. Повторне налаштування

При спробі налаштувати заблокований канал:
```
⚠️ Ваш канал був заблокований через зміну налви/опису/фото.

Будь ласка, не змінюйте налаштування каналу в майбутньому.
Продовжуємо налаштування...
```

## Технічна реалізація

### База даних

Додані поля до таблиці `users`:

| Поле | Тип | Опис |
|------|-----|------|
| `channel_title` | TEXT | Повна назва каналу |
| `channel_description` | TEXT | Повний опис каналу |
| `channel_photo_file_id` | TEXT | File ID фото для перевірки |
| `channel_user_title` | TEXT | Частина назви від користувача |
| `channel_user_description` | TEXT | Опціональний опис від користувача |
| `channel_status` | TEXT | 'active' або 'blocked' |

### Нові модулі

#### `src/channelGuard.js`
- `initChannelGuard(bot)` - ініціалізація щоденної перевірки
- `verifyAllChannels()` - перевірка всіх каналів
- `verifyChannelBranding(user)` - перевірка окремого каналу
- `checkExistingUsers(bot)` - міграція існуючих користувачів

#### Оновлені модулі

**`src/handlers/channel.js`:**
- `handleSetChannel()` - нова команда /setchannel
- `handleConversation()` - обробка діалогу з користувачем
- `handleChannelCallback()` - обробка кнопок
- `applyChannelBranding()` - застосування брендування
- `handleCancelChannel()` - скасування налаштування

**`src/database/users.js`:**
- `resetUserChannel()` - скидання інформації про канал
- `updateChannelBranding()` - оновлення брендування
- `updateChannelStatus()` - зміна статусу каналу
- `getUsersWithActiveChannels()` - отримання активних каналів
- `getUsersWithChannelsForVerification()` - канали для перевірки

**`src/scheduler.js`:**
- Додана перевірка `channel_status === 'blocked'`
- Заблоковані канали пропускаються при публікації

### Команди

| Команда | Опис |
|---------|------|
| `/channel` | Інформація про підключення каналу |
| `/setchannel @channel` | Налаштування каналу з брендуванням |
| `/cancel` | Скасування процесу налаштування |

### Необхідні права бота

Бот повинен бути адміністратором каналу з правами:
- ✅ Публікація повідомлень (`can_post_messages`)
- ✅ Редагування інформації каналу (`can_change_info`)

## Міграція існуючих користувачів

При запуску бота автоматично виконується:

1. Пошук каналів без правильного брендування
2. Блокування таких каналів
3. Відправка повідомлення про необхідність перенастройки:

```
⚠️ Оновлення GridBot!

Тепер всі канали мають використовувати стандартний формат:
- Назва: GridBot ⚡️ {ваша назва}
- Фото: стандартне фото GridBot

🔴 Моніторинг вашого каналу зупинено.

Щоб продовжити роботу, налаштуйте канал заново 
командою /setchannel
```

## Тестування

Запустіть тестовий скрипт:
```bash
node test-channel-branding.js
```

Тести перевіряють:
- ✅ Схему бази даних
- ✅ Наявність методів
- ✅ Експорти обробників
- ✅ Функції захисту каналів
- ✅ Наявність файлу фото
- ✅ Визначення констант

## Безпека

- ✅ CodeQL аналіз пройдено без попереджень
- ✅ Перевірено права доступу до каналу
- ✅ Валідація введення користувача
- ✅ Обробка помилок Telegram API
- ✅ Захист від race condition з bot.options.id

## Приклад використання

```javascript
// Користувач починає налаштування
/setchannel @my_power_channel

// Бот перевіряє права і запитує назву
"📝 Введіть назву для каналу"

// Користувач вводить
Київ Черга 3.1

// Бот пропонує додати опис
[✍️ Додати опис] [⏭️ Пропустити]

// Користувач натискає "Додати опис"
"Введіть опис:"

// Користувач вводить
ЖК "Сонячний", під'їзд 2

// Бот встановлює брендування і підтверджує
✅ Канал успішно налаштовано!

⚠️ УВАГА: Не змінюйте назву, опис або фото каналу!
```

## Відомі обмеження

1. Для зміни назви, опису та фото каналу потрібні спеціальні права адміністратора
2. Бот не може змінити канали, в яких він не є адміністратором
3. Перевірка виконується раз на добу (можна налаштувати інший інтервал)
4. File ID фото може змінитись при оновленні фото через Telegram API

## Підтримка

При виникненні проблем:
1. Перевірте права бота в каналі
2. Переконайтесь, що бот є адміністратором
3. Перевірте логи бота на помилки
4. Спробуйте налаштувати канал заново командою `/setchannel`
