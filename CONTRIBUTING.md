# Участь у розробці

Дякуємо за інтерес до проєкту! 🎉

## Як допомогти

### Повідомити про помилку

Якщо ви знайшли помилку:
1. Перевірте, чи немає вже подібного [issue](https://github.com/Ivan200424/eSvitlo-monitor-bot/issues)
2. Створіть новий issue з детальним описом
3. Додайте кроки для відтворення проблеми
4. Вкажіть версію Node.js та операційну систему

### Запропонувати нову функцію

Маєте ідею? Чудово!
1. Створіть issue з описом функції
2. Поясніть навіщо вона потрібна
3. Дочекайтесь обговорення перед реалізацією

### Надіслати Pull Request

1. Форкніть репозиторій
2. Створіть нову гілку:
   ```bash
   git checkout -b feature/amazing-feature
   ```
3. Внесіть зміни
4. Перевірте код:
   ```bash
   npm start  # переконайтесь, що все працює
   ```
5. Закомітьте зміни:
   ```bash
   git commit -m "Додано чудову функцію"
   ```
6. Запуште гілку:
   ```bash
   git push origin feature/amazing-feature
   ```
7. Створіть Pull Request

## Стиль коду

- Використовуйте 2 пробіли для відступів
- Коментарі та повідомлення українською мовою
- Назви змінних та функцій англійською у camelCase
- Додавайте коментарі до складної логіки

## Структура коміту

```
Тип: короткий опис

Детальний опис змін (опціонально)
```

Типи:
- `feat`: нова функція
- `fix`: виправлення помилки
- `docs`: зміни в документації
- `style`: форматування коду
- `refactor`: рефакторинг
- `test`: додавання тестів
- `chore`: інші зміни

## Тестування

Перед відправкою PR:
1. Переконайтесь, що код не має синтаксичних помилок
2. Протестуйте основні функції бота
3. Перевірте, що нові функції працюють як очікується

## Питання?

Створіть issue або напишіть в обговореннях!

Дякуємо за ваш внесок! ❤️
